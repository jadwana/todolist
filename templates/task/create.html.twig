{% extends 'base.html.twig' %}

{% block body %}
<div>
    <a href="{{ path('task_list') }}" class="mt-3 mb-5 btn btn-primary">Retour à la liste des tâches</a>
</div>
<div class="row">
    {{ form_start(form, {'action' : path('task_create')}) }}
        {{ form_widget(form) }}
         <button type="submit" class="btn btn-success float-end">Ajouter</button>
    {{ form_end(form) }}
        
</div>
{% endblock %}
